[theme]
primaryColor = "#3151CB"
backgroundColor = "#F3F3F0"
secondaryBackgroundColor = "#F8F8F5"
textColor = "#171D27"
font = "sans serif"

[client]
# 对访客隐藏开发者选项（Rerun / Clear cache / 源码入口等）
toolbarMode = "viewer"
# 出错时只显示简短提示，不向访客暴露堆栈与代码片段
showErrorDetails = false

[server]
runOnSave = true
